# Schema for the configuration files of the reqresimport module.
reqresimport.settings:
  type: config_object
  label: 'reqresimport settings'
  mapping:
    example:
      type: string
      label: 'Example'
